
<% if match.date >= DateTime.now %>

  <%= render '/matches/lwc_semi_final_match/open',
      match: match, context: context %>

<% else %>

  <%= render '/matches/lwc_semi_final_match/closed',
      match: match, context: context %>

<% end %>


