{{#my-modal title=title isPresent=isPresent close='removeModal'}}
  {{#if isLoginModal}}
    {{partial "authentication-modal/login"}}
  {{else}}
    {{partial "authentication-modal/signup"}}
  {{/if}}
{{/my-modal}}
